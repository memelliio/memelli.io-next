(function(){var canvas=document.getElementById('scene');if(!canvas)return;var gl=canvas.getContext('webgl2',{preserveDrawingBuffer:true});if(!gl)return;var NL=String.fromCharCode(10);var vsSrc='#version 300 es'+NL+'in vec3 aSeed;'+NL+'uniform float uTime;'+NL+'uniform float uSpeed;'+NL+'out float vSeed;'+NL+'void main(){'+NL+'vec2 p=aSeed.xy;float ang=uTime*0.06;vec2 c=p-0.5;vec2 rp=vec2(c.x*cos(ang)-c.y*sin(ang),c.x*sin(ang)+c.y*cos(ang))+0.5;vec2 d=fract(rp+vec2(sin(p.x*6.28),cos(p.y*6.28))*uTime*uSpeed);gl_Position=vec4(d*2.0-1.0,0,1);gl_PointSize=2.0;vSeed=aSeed.z;}';var fsSrc='#version 300 es'+NL+'precision mediump float;'+NL+'in float vSeed;'+NL+'uniform float uTime;'+NL+'out vec4 outColor;'+NL+'void main(){'+NL+'float d=length(gl_PointCoord-vec2(0.5))*2.0;float a=smoothstep(1.0,0.0,d);vec3 col=vec3(0.85,0.11,0.17);float sp=step(0.99,vSeed)*0.5;col=mix(col,vec3(1.0),sp);outColor=vec4(col,a*0.42);}';function cS(t,s){var sh=gl.createShader(t);gl.shaderSource(sh,s);gl.compileShader(sh);return gl.getShaderParameter(sh,gl.COMPILE_STATUS)?sh:null;}var vS=cS(gl.VERTEX_SHADER,vsSrc),fS=cS(gl.FRAGMENT_SHADER,fsSrc);if(!(vS&&fS))return;var prog=gl.createProgram();gl.attachShader(prog,vS);gl.attachShader(prog,fS);gl.linkProgram(prog);if(!gl.getProgramParameter(prog,gl.LINK_STATUS))return;gl.useProgram(prog);var cnt=110000;var data=new Float32Array(cnt*3);for(var i=0;i<data.length;i++)data[i]=Math.random();var buf=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buf);gl.bufferData(gl.ARRAY_BUFFER,data,gl.STATIC_DRAW);var aLoc=gl.getAttribLocation(prog,'aSeed');var uTime=gl.getUniformLocation(prog,'uTime');var uSpeed=gl.getUniformLocation(prog,'uSpeed');gl.enable(gl.BLEND);gl.blendFunc(gl.ONE,gl.ONE);var logoBuf=null,logoProg=null,aPos=null,uTimeL=null,maxLogo=0;var logoImg=new Image();logoImg.crossOrigin='anonymous';try{fetch('/api/demo/frames?surface='+encodeURIComponent('*infinity*surface*stage*demo*')+'&w=1024&h=1024&n=90000&palette=gpu_real').then(function(r){return r.json();}).then(function(j){if(j&&j.frames&&j.frames.length&&j.frames[0]){logoImg.src=j.frames[0];}else{logoImg.src='/api/brand/logo';}}).catch(function(){logoImg.src='/api/brand/logo';});}catch(e){logoImg.src='/api/brand/logo';}logoImg.onload=function(){try{var off=document.createElement('canvas');off.width=512;off.height=512;var oc=off.getContext('2d');oc.drawImage(logoImg,0,0,512,512);var im=oc.getImageData(0,0,512,512).data;var pts=[];for(var y=0;y<512;y+=3){for(var x=0;x<512;x+=3){if(im[(y*512+x)*4+3]>60){pts.push(x/512,y/512,Math.random());}}}logoProg=gl.createProgram();var vsL='#version 300 es'+NL+'in vec3 aPos;'+NL+'uniform float uTime;'+NL+'out float vA;'+NL+'void main(){'+NL+'vec2 q=vec2(aPos.x-0.5,(1.0-aPos.y)-0.5)*0.62;float e=0.5+0.5*sin(uTime*0.5);vec2 dir=vec2(-1.4-abs(sin(aPos.z*40.0)),cos(aPos.z*78.9)*0.5);vec2 f=mix(q+dir*1.2,q,e);gl_Position=vec4(f,0.0,1.0);gl_PointSize=3.0;vA=e;}';var fsL='#version 300 es'+NL+'precision mediump float;'+NL+'in float vA;'+NL+'out vec4 o;'+NL+'void main(){'+NL+'float d=length(gl_PointCoord-vec2(0.5))*2.0;float a=smoothstep(1.0,0.0,d);o=vec4(0.82,0.13,0.20,a*0.98*vA);}';var vSL=cS(gl.VERTEX_SHADER,vsL),fSL=cS(gl.FRAGMENT_SHADER,fsL);if(!(vSL&&fSL))return;gl.attachShader(logoProg,vSL);gl.attachShader(logoProg,fSL);gl.linkProgram(logoProg);if(!gl.getProgramParameter(logoProg,gl.LINK_STATUS))return;aPos=gl.getAttribLocation(logoProg,'aPos');uTimeL=gl.getUniformLocation(logoProg,'uTime');maxLogo=Math.min(40000,Math.floor(pts.length/3));var ld=new Float32Array(maxLogo*3);for(var k=0;k<maxLogo;k++){ld[k*3]=pts[k*3];ld[k*3+1]=pts[k*3+1];ld[k*3+2]=Math.random();}logoBuf=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,logoBuf);gl.bufferData(gl.ARRAY_BUFFER,ld,gl.DYNAMIC_DRAW);}catch(e){}};function resize(){var r=Math.min(2,devicePixelRatio);canvas.width=innerWidth*r;canvas.height=innerHeight*r;gl.viewport(0,0,canvas.width,canvas.height);}resize();addEventListener('resize',resize);var __spin=1,__density=1,__morph=0,__auto=1;window.__scene={setSpin:function(v){__spin=+v;},setDensity:function(v){__density=Math.max(0.05,Math.min(1,+v));},setMorph:function(v){__auto=0;__morph=Math.max(0,Math.min(1,+v));},setGlow:function(v){canvas.style.filter='brightness('+Math.max(0,Math.min(2,+v))+')';},play:function(){},get state(){return __morph<0.02?1:(__morph>0.98?3:2);},get morph(){return __morph;}};var t0=performance.now();(function render(){var t=(performance.now()-t0)*0.001;if(__auto){var __ph=(t%9.6)/9.6;var __m=__ph<0.25?0.0:(__ph<0.5?(0.5-0.5*Math.cos((__ph-0.25)/0.25*Math.PI)):(__ph<0.75?1.0:(0.5+0.5*Math.cos((__ph-0.75)/0.25*Math.PI))));__morph=__m;}gl.clearColor(0,0,0,1);gl.clear(gl.COLOR_BUFFER_BIT);gl.useProgram(prog);gl.uniform1f(uTime,t*__spin);gl.uniform1f(uSpeed,0.15*__spin);gl.bindBuffer(gl.ARRAY_BUFFER,buf);gl.enableVertexAttribArray(aLoc);gl.vertexAttribPointer(aLoc,3,gl.FLOAT,false,0,0);gl.drawArrays(gl.POINTS,0,Math.round(cnt*__density));if(logoProg&&logoBuf){gl.useProgram(logoProg);gl.bindBuffer(gl.ARRAY_BUFFER,logoBuf);gl.enableVertexAttribArray(aPos);gl.vertexAttribPointer(aPos,3,gl.FLOAT,false,0,0);gl.uniform1f(uTimeL,t*__spin);gl.drawArrays(gl.POINTS,0,Math.round(maxLogo*__density*__morph));}requestAnimationFrame(render);})();})();
;
(function(){var c=document.getElementById("scene-text");if(!c)return;var gl=c.getContext("webgl2",{alpha:true,premultipliedAlpha:true});if(!gl)return;var NL=String.fromCharCode(10);function cS(t,s){var sh=gl.createShader(t);gl.shaderSource(sh,s);gl.compileShader(sh);return gl.getShaderParameter(sh,gl.COMPILE_STATUS)?sh:null;}var vs="#version 300 es"+NL+"in vec2 aPos;uniform float uScale;uniform float uAspect;void main(){vec2 p=aPos*uScale;p.x*=uAspect;gl_Position=vec4(p,0.0,1.0);gl_PointSize=2.3;}";var fs="#version 300 es"+NL+"precision mediump float;uniform float uGlow;out vec4 o;void main(){float d=length(gl_PointCoord-vec2(0.5))*2.0;float a=smoothstep(1.0,0.0,d);o=vec4(0.96,0.2,0.26,a*0.92*uGlow);}";var v=cS(gl.VERTEX_SHADER,vs),f=cS(gl.FRAGMENT_SHADER,fs);if(!(v&&f))return;var prog=gl.createProgram();gl.attachShader(prog,v);gl.attachShader(prog,f);gl.linkProgram(prog);if(!gl.getProgramParameter(prog,gl.LINK_STATUS))return;var aPos=gl.getAttribLocation(prog,"aPos"),uScale=gl.getUniformLocation(prog,"uScale"),uAspect=gl.getUniformLocation(prog,"uAspect"),uGlow=gl.getUniformLocation(prog,"uGlow");var buf=gl.createBuffer();var N=0,sc=0.8,gw=1.0;function rz(){var r=Math.min(2,devicePixelRatio||1);c.width=innerWidth*r;c.height=innerHeight*r;gl.viewport(0,0,c.width,c.height);}rz();addEventListener("resize",rz);gl.enable(gl.BLEND);gl.blendFunc(gl.SRC_ALPHA,gl.ONE);function build(str){str=(str||"").trim();if(!str){N=0;return;}var off=document.createElement("canvas");off.width=1024;off.height=256;var x=off.getContext("2d");x.clearRect(0,0,1024,256);x.fillStyle="#fff";x.textAlign="center";x.textBaseline="middle";var fz=150;x.font="800 "+fz+"px Inter,system-ui,sans-serif";while(x.measureText(str).width>980&&fz>16){fz-=4;x.font="800 "+fz+"px Inter,system-ui,sans-serif";}x.fillText(str,512,128);var im=x.getImageData(0,0,1024,256).data;var pts=[];for(var yy=0;yy<256;yy+=2){for(var xx=0;xx<1024;xx+=2){if(im[(yy*1024+xx)*4+3]>128){pts.push((xx-512)/512,-(yy-128)/512);}}}N=pts.length/2;gl.bindBuffer(gl.ARRAY_BUFFER,buf);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(pts),gl.STATIC_DRAW);}(function loop(){gl.clearColor(0,0,0,0);gl.clear(gl.COLOR_BUFFER_BIT);if(N>0){gl.useProgram(prog);gl.uniform1f(uScale,sc);gl.uniform1f(uAspect,c.height/c.width);gl.uniform1f(uGlow,gw);gl.bindBuffer(gl.ARRAY_BUFFER,buf);gl.enableVertexAttribArray(aPos);gl.vertexAttribPointer(aPos,2,gl.FLOAT,false,0,0);gl.drawArrays(gl.POINTS,0,N);}requestAnimationFrame(loop);})();window.__text={render:function(s){build(s);},setSize:function(val){sc=+val;},setGlow:function(val){gw=+val;}};})();